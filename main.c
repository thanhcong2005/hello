#include <stdio.h>
#include <stdlib.h>


/*
    Toan tu toan hoc: + - * / % ++ --
    Toan tu so sanh: == != > >= < <=
    Toan tu logic && || !
    Toan tu gan: = += *= -= %=
*
int main()
{
    int a =100 ,b =200;
    int c,d;
    c =a/b;
    c = ++a;
    a = 100;
    d = a++;
    a || b;
        printf("c = %d",c);
        printf("c = %d",d);
    return 0;
}

